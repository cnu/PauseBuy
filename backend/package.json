{
  "name": "pausebuy-backend",
  "version": "0.1.0",
  "description": "PauseBuy proxy backend for Claude API calls",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy",
    "lint": "eslint api lib --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@vercel/kv": "^3.0.0",
    "opik": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0",
    "vercel": "^39.0.0"
  }
}
