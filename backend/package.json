{
  "name": "pausebuy-backend",
  "version": "0.1.0",
  "description": "PauseBuy proxy backend for OpenAI GPT-5 Mini API calls",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "dev:local": "tsx --env-file=.env.local server.ts",
    "build": "vercel build",
    "deploy": "vercel deploy",
    "lint": "eslint api lib --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "openai": "^4.77.0",
    "opik-openai": "^1.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/node": "^3.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vercel": "^39.0.0"
  }
}
